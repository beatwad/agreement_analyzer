version: "3.8"

services:
  backend:
    build: ./backend
    container_name: agreement_analyzer_backend
    ports:
      - "8001:8001"
    volumes:
      - ./backend:/app
    restart: unless-stopped
